name: First Workflow

on: [push]

jobs: 
  run-shell-cmd:
    runs-on: ubuntu-latest
    steps:
      - name: echo world
        run : echo "hello, world"
      - name: execute multiline script
        run:
          ls -al
          yarn -v
          



